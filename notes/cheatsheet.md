# Cheatsheet

Syntax only. No explanations.

## CSS

```css
display: flex;
justify-content: ;
align-items: ;
flex-direction: ;
gap: ;

display: grid;
grid-template-columns: ;
```

## JavaScript

```js
const res = await fetch(url);
const data = await res.json();
```

## HTMX

```html
<button hx-get="/tickets" hx-target="#list" hx-swap="innerHTML">
```

## React

```tsx
const [value, setValue] = useState(initial);
```
