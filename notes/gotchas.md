# Gotchas

Only what actually bites you. Add a line when you hit it twice.

## JavaScript

- `==` vs `===`
- `undefined` vs `null`
- `this` depends on the call site
- Objects and arrays are references

## TypeScript

- Types are erased at runtime — validate API JSON yourself

## CSS

-

## HTMX

-

## React

- Hooks only at the top level
- Do not mutate state
- `useEffect` dependency array
- Keys on lists
